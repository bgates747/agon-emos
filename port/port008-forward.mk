# SUPERSEDED predecessor profile — evidence only.
#
# INTEG-002 replaced the fixed-purpose r01 GPIO sender with the common
# production parallel epoch/route objects and the separately identified
# parallel-fixed-qualification profile.  Keeping this filename makes old logs
# and manifests intelligible; selecting it must fail instead of silently
# rebuilding an obsolete precommit-General-Poll transport.
$(error port/port008-forward.mk is SUPERSEDED; use port/parallel-fixed-qualification.mk)
