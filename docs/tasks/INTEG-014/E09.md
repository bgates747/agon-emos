# E09 — Paired rendering load after UART parity

## Executive summary

The later unattended run supplies a complete passing baseline comparison:
624 intervals, all status zero, eight exactly unchanged probe differences and
zero P4 snapshots. Largest bitmap stage: 866.038ms P4 versus883.707ms mainboard,
priorP43048.353ms. Original firmware/startup and CLI/SD recovery are verified.
[Full result and tables](../../../../agon-extender/docs/tasks/QUAL-003/timing/results/e09-unattended/README.md).
The earlier timeout/partial run remain evidence; E10 is not started.

Author authorized E09 after E08 completion on 2026-09-14. Repeat the unchanged
39-case paired graphics fixture with the qualified ordinary E07P EMOS and P4
owner-loop improvement. Separate upload, resident draw and output; P4 browser
output stays off. No new renderer/protocol optimization is authorized here.

## Execution contract

1. [x] Preserve current bench identities/startup and verify rollback artifacts.
   Pin ordinary E07P EMOS, P4 owner01 and existing mainboard graphics probe.
   Freeze this plan before physical changes; no new firmware source changes.
2. [x] Install and independently verify those exact diagnostic candidates;
   recover fresh keyboard/SD admission. Stage the unchanged 39-case fixture.
   Modes are selected by the external launch script, never inside the fixture.
3. [x] Run the frozen paired matrix and collect its terminal CSV. Preserve
   probe differences and any timeout. One unchanged retry after explicit reset
   is permitted by the prior procedure; no case removal or timeout extension.
4. [x] Validate sequence/count/byte metadata; compare worst-first milliseconds
   and elapsed percentages against stock and prior EDP. Keep unequal sprite
   batching and observer limits explicit. Confirm zero P4 snapshots.
5. [x] Restore exact pre-run firmware/startup, verify keyboard/CLI/SD recovery,
   record findings and commit discrete results. Hardware voice, then stop for
   review; E10 full production qualification is not silently claimed.

Prior references: Extender PORT-008/uart-alignment/FINDINGS.md U11 and
results/graphics-comparison.md; graphics02 contains the successful 624-interval
matrix, graphics01 the unresolved mainboard BSP21_01 timeout. The eight SHP23
probe differences are known evidence, not permission to ignore new failures.
The E07P frozen ordinary image includes parallel support; the size-only
parallel-disabled experiment is never a deployment candidate (its omitted feature
was parallel, not UART). No wiring changes, browser viewer, emulator launch,
Golem or gameplay work. If diagnosis requires substantive changes, preserve
facts and request human attention rather than expand this test contract.

## Installation and first run

Original EMOS full-ROM hash matches the E07P restoration. Ordinary candidate
429f85eb... is installed and independently read back; original mainboard flash
is freshly preserved/verified. P4 exact pre-run 2 MiB prefix matches before
owner01 deployment, followed by independent flash verification and USB startup.
Both diagnostic ESP images and fresh EMOS/SD admission passed.

First run GQT007.CSV stopped at mainboard route0 BSP21_01, status15,
submission48/reply-wait1200 ticks, 15 saved intervals. This matches the prior
failing operation, but at an earlier repeat. Partial evidence is retained.
One unchanged retry is authorized. Resetting VDP invalidated keyboard admission;
no retry fixture launched during that setup interruption. A Pi reset restored
EMOS/SD, followed by a fresh keyboard session without replaying old input.
The unchanged fixture was read back again before actual retry launch.

## Retry outcome

The actual unchanged retry exceeded the existing 1200-second collector limit.
No third test was started. Progress endpoint snapshots stayed at zero; rising
parser/TX counters alone cannot identify completed graphics cases. Pi reset
re-established keyboard admission but not foreground SD service, even after
explicit CLI launch. Restore original ESP images before further EMOS recovery.
[Retained outcome](E09-results/outcome.json); no root cause is claimed.

## Recovered partial result

After restoring both original ESP images and resetting EMOS, SD service returned.
Recovered GQT008.CSV contains 550/624 intervals (4400 metric rows), all status0,
ending at repeat3/P4/SHP20 output probes, without a terminal record. This is a
collection-deadline failure, not evidence of an explicit second draw timeout.
Do not call it a proven firmware hang or blame EMOS without further diagnosis.

[Exploratory tables](E09-results/partial-comparison.md) compare only complete
matched repeats1 and2, two samples per route. Largest bitmap BSP03_01 elapsed:
mainboard882.518ms, P4864.936ms, prior P43048.396ms. Primitive work is separately
37.581ms versus11.489ms. These are encouraging partial observations, not a
completed regression or rendering parity qualification. Unequal sprite batching
is not converted into a false per-sprite comparison. No output timing claim.

## Restored review checkpoint

Exact original EMOS ROM and both original ESP app regions are restored and
verified. Startup and previous backup are unchanged; SD roundtrip and actual
keyboard-launched CLI COPY pass. Neutral Legacy MOS prompt. No wiring change,
third graphics run, emulator launch or new firmware source change occurred.
[Restoration receipt](E09-results/restoration.json). E09 remains open: execution
and failed-result analysis are done, but the required complete matrix did not
pass. Request Author disposition before further diagnosis or E10.

Hardware voice completed with a fresh service receipt; returned to MOS.
Stop for Author review. E09 is not marked passed. Nothing pushed.

## Author-directed unattended revision — 2026-09-14

This supersedes the host collection deadline for the NEXT run, not historical
results. Extender owns the fixture implementation and its detailed preparation
contract in `docs/tasks/QUAL-003/timing/unattended.md`.

1. Keep the same 39 cases, order, four repeats, measured payloads, per-operation
   timeouts and diagnostic firmware. No renderer or EMOS product source edits.
2. Save pre-operation checkpoints on the mainboard SD, explicitly sync/close;
   retain interval records and terminal status. Host monitoring is unnecessary.
3. Print a scrolling mainboard log during P4 work, outside measured intervals;
   include repeat/case/phase/count. Do not clear previous status messages.
4. After normal or detected-error return, play the accepted voice recording
   with a non-clearing player and return to foreground SD service. A hard hang
   cannot execute a completion alert; the last SD checkpoint remains evidence.
5. Build/check/stage only now, then hardware attention notification. The Author
   will arrange a 30-minute human check when the next launch is authorized.
   Do not start E10, run the matrix, or impose an automatic host reset deadline.

The Author subsequently authorized launch of the prepared unattended revision.
Verify the same pinned diagnostic images and launch prerequisites, then issue
one invocation and cease host monitoring. No collector, automatic deadline,
result retrieval or restoration after launch until the Author returns. The
fixture owns durable progress and completion voice; the Author owns the phone
timer and visual progress check. No complete-run result is claimed by launch.


## Completed unattended follow-up

Author authorized r02 launch without host monitoring and subsequently reported
completion with about1m30s left on a30-minute phone timer started a few minutes
late. GQT009 validates624intervals/4992metric rows and1560progress records. All
8probe mismatch signatures exactly match the prior complete baseline. ZeroP4
snapshots. All8upload-containing stages are faster than mainboard; worst elapsed
excess is0.632ms/3.86% onBSP22_03. Mainboard output and P4 framebuffer-only work
remain unequal output scopes, so no display FPS or scanout qualification.

The nominal MOS-clock span is27m13.267s; the Author's observation suggests over
30minutes. Treat this as a clock/scope limitation, not an exact wall duration.
A future undeployed r03 adds explicit elapsed ticks. Independent clock validation
belongs before precise completion estimates; roughly35minutes is only a planning
allowance for this same suite. The20-minute cutoff must not be reused blindly.

Original EMOS fullROM and bothESP images restored/verified; fresh keyboard/CLI,
SD read/write and rootstartup pass. E09's bounded execution is complete. Await
Author review and explicit E10 authorization; do not claim production parity.
