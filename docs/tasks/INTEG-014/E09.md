# E09 — Paired rendering load after UART parity

## Executive summary

Author authorized E09 after E08 completion on 2026-09-14. Repeat the unchanged
39-case paired graphics fixture with the qualified ordinary E07P EMOS and P4
owner-loop improvement. Separate upload, resident draw and output; P4 browser
output stays off. No new renderer/protocol optimization is authorized here.

## Execution contract

1. [ ] Preserve current bench identities/startup and verify rollback artifacts.
   Pin ordinary E07P EMOS, P4 owner01 and existing mainboard graphics probe.
   Freeze this plan before physical changes; no new firmware source changes.
2. [ ] Install and independently verify those exact diagnostic candidates;
   recover fresh keyboard/SD admission. Stage the unchanged 39-case fixture.
   Modes are selected by the external launch script, never inside the fixture.
3. [ ] Run the frozen paired matrix and collect its terminal CSV. Preserve
   probe differences and any timeout. One unchanged retry after explicit reset
   is permitted by the prior procedure; no case removal or timeout extension.
4. [ ] Validate sequence/count/byte metadata; compare worst-first milliseconds
   and elapsed percentages against stock and prior EDP. Keep unequal sprite
   batching and observer limits explicit. Confirm zero P4 snapshots.
5. [ ] Restore exact pre-run firmware/startup, verify keyboard/CLI/SD recovery,
   record findings and commit discrete results. Hardware voice, then stop for
   review; E10 full production qualification is not silently claimed.

Prior references: Extender PORT-008/uart-alignment/FINDINGS.md U11 and
results/graphics-comparison.md; graphics02 contains the successful 624-interval
matrix, graphics01 the unresolved mainboard BSP21_01 timeout. The eight SHP23
probe differences are known evidence, not permission to ignore new failures.
The E07P frozen ordinary image includes parallel support; the size-only
parallel-disabled experiment is never a deployment candidate (its omitted feature
was parallel, not UART). No wiring changes, browser viewer, emulator launch,
Golem or gameplay work. If diagnosis requires substantive changes, preserve
facts and request human attention rather than expand this test contract.
