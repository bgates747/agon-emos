# E08 — Correctness stress consolidation

## Executive summary

All host and linked checks pass; unchanged final E07P images retain their
verified physical exact/mixed evidence. Fresh restored-bench SD/CLI checks
pass. No firmware change or flash was needed.

Author authorized E08 on 2026-09-14, with hardware spoken notification at
completion or need for attention. Execute the existing correctness contract
with economical evidence reuse; E09/E10 are not started by this authorization.
No new firmware optimization, protocol change or production feature is planned.

## Procedure

1. [x] Map E08 requirements to existing host, actual-instruction and physical
   evidence, verifying the qualified candidate identities and current source.
2. [x] Run the host suite and final frozen ordinary/bench linked interrupt and
   sender checks. Reuse exhaustive parser evidence where exact images match;
   add a narrowly scoped missing check only if the coverage audit finds a gap.
3. [x] Consolidate E07P final physical exact/mixed-load results for unchanged
   candidates. These are reused physical results, not new runs. Do not reflash
   three working processors solely to duplicate already-passing same-image tests.
   Check current bench admission, CLI/SD recovery and neutral input live.
4. [x] Record coverage and limitations, commit task-owned evidence, then send
   the established hardware voice cue and stop before E09 for Author review.

Every item must identify its evidence layer. Host C models are not actual eZ80
instruction execution; isolated CPU boundaries are not whole-machine interrupt
scheduling; prior physical results are not fresh tests of the restored firmware.
If a correctness failure calls for substantive firmware diagnosis, preserve the
failure and request attention rather than drifting into a new optimization run.
The UART-only size experiment is excluded: E08 targets full parallel-included
E07P ordinary and bench images. Existing unrelated budget documentation will
be preserved separately. No experimental push or emulator changes.

## Coverage and findings

| E08 requirement | Evidence | Result and scope |
|---|---|---|
| Parser boundaries, malformed/partial packets, bounded storage | Fresh final bench-image public/private instruction comparisons | 8,683,703 public and 8,582,356 IRQ-admitted byte comparisons pass |
| Complete IRQ, callback faults/ownership changes, register preservation | Fresh final-image linked IRQ comparisons | 3,906 bench and 1,302 ordinary cases pass; primary/shadow registers, stack, IFF and 16-byte FIFO bound |
| CTS/THRE stalls, error ordering, timeout, clock wrap/stoppage, partial output | Fresh linked sender comparisons | 375 cases per profile pass; active-low GPIO CTS and port traces compared |
| Source changes, callback-edited keys, releases, stale frames, IRQ-off admission | Fresh ASan/UBSan host keyboard/transmit harnesses, both compositions | Pass; real maintained C reference and scripted device/IRQ boundaries |
| Independent mainboard scratch, sysvar publication, routing/leases, stale replies | Fresh console and mode-transaction host checks | Pass; mainboard 16-byte scratch preserved across Extender effects; host boundary proof, not simultaneous physical parser scheduling |
| SD/telemetry lifecycle and bounded mailboxes | Fresh complete host suite | All 91 tests pass |
| Exact-byte/mixed physical regression | Reused final E07P ordinary and bench records | 384 cases each; all 10 corresponding CSV hashes verified; six independent captures across profiles |

Fresh linked checks use the exact frozen full-profile binaries, not the
UART-only budget experiment. All saved maintained `src/` inputs match current
source. Physical evidence identifies bench SHA256
`60729c6a7bd6d953e2311b933247b56b7e04bbf47c4e374058c8a82df22583ce`
and ordinary SHA256
`429f85ebb413bf3eed779307a71126b6cafb7bf884ad1824c6673e1814718b61`.
Their exact/mixed tests, return batches and restoration are recorded in
[E07P results](E07P-results/README.md). No performance data are relabelled as
fresh E08 measurements.

The audit found no uncovered requirement that calls for new firmware code in
this increment. Deterministic error injection is supplied by host and linked
instruction tests; it does not claim that every electrical fault was induced
on the physical bench. Whole-machine exhaustive interrupt scheduling and new
human keyboard/gameplay acceptance are not established. E09 rendering remains
separate and E10 retains its human acceptance/publication boundary.

Machine-local command driver and fresh logs: `build/integ-014/E08/`.
The linked drivers consume final image directories and the pinned E07P CPU
harness; `check.py` verifies source/image identities and runs both profiles.
`evidence.py` checks the reused physical CSV hashes. The host suite runs with
`MOS_AGONDEV_WORKTREE` pointing to the frozen ordinary prepared source and
`.venv/bin/python -m unittest discover -s tests -v`.

## Live bench checkpoint

Fresh P4 keyboard admission matched the previous ready session. A 4,097-byte
SD upload/download and actual keyboard-launched MOS `COPY` passed. Original
startup and backup remain byte-exact; spoken alert assets match their accepted
hashes. The bench remains on the restored original firmware, at a neutral
Legacy MOS prompt. No reset, flash, browser output or wiring change occurred.
[Portable service receipt](E08-results/services.json).

Hardware voice completed with a fresh service receipt, then returned to MOS.
Human hearing remains unconfirmed. E08 complete; stop before E09.
