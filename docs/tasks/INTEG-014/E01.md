# E01 — Preserved inputs and recovery checkpoint

Completed 2026-09-14 UTC (2026-09-13 local). Supervised stop before E02.
Contract frozen in EMOS `43bc9c0`; Extender queue link frozen in `af6e60e`.

1. Preserved a complete tracked/nonignored EMOS source archive, including
   uncommitted telemetry sources/tests/profile, and verified every archived
   file against its SHA-256 manifest. Original files remain in place. Saved
   binary Git patches for EMOS, Extender and mos-agondev; additionally archived
   mos-agondev's untracked source/test records. No preexisting source work was
   committed as part of this task.
2. Recorded source revisions, current dirty status, ordinary/bench profile
   hashes and compiler/linker/objdump identities. EMOS baseline is `af20d4e`,
   Extender `255df4c`, generic port `2b7884c`. The installed toolchain resolves
   to AgonDev Clang 15.0.7 and Binutils 2.45; E02 must establish reproducibility
   rather than assume that version labels imply identical historical output.
3. Official reference trees are clean: MOS v3.0.2 at `8336409`, VDP v2.16.0
   at `c7ac293`, documentation at `f9806bd`.
4. Rehashed the saved EMOS v0.1.16 binary/ELF/map against its manifest. Rehashed
   the original P4 factory rollback against its verified deployment receipt;
   rehashed the actual mainboard 4 MiB flash backup. All match. Existing
   restoration scripts and independent verification receipts remain preserved.
5. Live keyboard status matched the preceding verified boot/session, ready,
   neutral and with no keys held. Through the established keyboard CLI path,
   loaded foreground sdserve and ran it with the whole-card root. Downloaded
   root startup and its previous backup; both are byte-identical to the prior
   restoration record. No startup changes.
6. Uploaded, activated and read back a new 257-byte deterministic proof at
   `/extender/gqt/i14-e01.dat`; all bytes match. SHA-256:
   `dd31e55fd954d9edc9e29f8e078387f0dd38e8ea832376311820de6322cdb0d1`.
   Requested clean service exit with no pending transfer, returning to the
   ordinary CLI for the hardware notification.

Evidence remains ignored: EMOS `build/integ-014/E01-2026-09-14-01-26-36Z/`
contains source archives, patches and manifest; Extender
`agents/integ-014/E01-2026-09-14-01-26-36Z/` contains the checks, SD journal and
startup readbacks. Established CLI tool journals record command delivery,
not independent screen or audible-output observation.

No firmware changes, builds, resets, serial opens, browser/video capture or
wiring changes were performed. The installed firmware identity relies on
previous physical verification plus the unchanged live boot epoch; this is
not a fresh flash readback. Fresh candidate verification is required before
later deployment. No timing or gameplay validation is claimed.

Next checkpoint: E02, reproduce ordinary/bench builds and itemise flash and
RAM budgets. Await the Author before starting it. No push performed.
