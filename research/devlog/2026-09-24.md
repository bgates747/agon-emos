# 2026-09-24 — Documentation reconciliation

The Author approved an unattended Extender documentation audit. This EMOS
tranche corrects the current listener/utility entry points and labels the old
provider-loader contract and initial migration handoff as historical. Retained
resident SD caller bounds now include the supported MOSlet region. Ownership
names distinguish the read-only canonical stock references from the separate
working MOS fork; no repository role is reassigned.

The sdserve README distinguishes ordinary application and MOSlet link layouts,
current `/emos/sdserve.bin` invocation, the already-admitted keyboard prerequisite,
checked/fast guarantees and dated hardware results. Current operations remain
owned by Extender's mainboard SD guide; this removes duplicate stale instructions.
Root README separates attributed upstream build/recovery prose from maintained
EMOS wrappers. No firmware, source, build, emulator, bench or SD state changed.

Local relative-link and whitespace checks were run. A misplaced INTEG-002 link
was repaired; the migration handoff's former-workspace links are explicitly
historical and point readers to current authorities. This is not a new hardware
qualification or renewed acceptance of old module-provider behavior.

The Author clarified that maintained manuals must contain only current guidance,
not require archaeological reconciliation. The resident contract is therefore
rewritten to the current 66-byte gateway ABI and supported services; cancelled
loader behavior is retained in Git instead. The documentation index and root
README now lead to current authorities. INTEG-009–012 queue/opening summaries
reflect later bounded hardware evidence without closing broader parity gates.
The completed REMOTE-005 MOSlet component is removed from this unfinished queue;
Extender still owns its wider networking/fast-transfer work. Current AGENTS
points readers to the same contracts. Source code and hardware remain unchanged.
