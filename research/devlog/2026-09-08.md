# Development log — 2026-09-08

## INTEG-004 — Hardware candidate preparation

The Author authorized the next hardware step after accepting the v0.3.0
graphical review and source checkpoint. Promote only lifecycle metadata to
candidate and build clean inputs through the existing complete gate. Stage
the guarded MOS-only updater, preserve the installed v0.2.0 payload, and keep
the P4 receive-only for the first physical missing-reply test. Both boards
stay powered with the harness seated under the Author's current instruction.
No firmware implementation or emulator launcher changes accompany promotion.

Built and checked candidate agon-emos-v0.3.0-b2026-09-08-04-04-00Z,
117828 bytes, SHA-256 371c1e9182b97d11be3c53fb1133b27bd4e566d9209d0b4c475e92c78561cf36.
The guarded installation card is prepared and unmounted; physical results
remain pending. Extender's design-adjacent preparation record owns the media
evidence and operator handover.

Author reports a good flash; the card's consumed payload matches v0.3.0.
The combined smoke/timeout media is staged and unmounted. Await hardware
smoke and no-peer result; no firmware changes or additional build occurred.

The Author screenshot confirms the exact installed v0.3.0 candidate,
physical SD/clock smoke, intentional missing-reply failure and prompt return.
Extender retains the passing observation beside r03's tests. No calibrated
timeout duration is inferred. The ACK test remains pending; no EMOS source,
build or media change is needed for it.

## INTEG-004 — Complete

The Author confirmed EMOS round-trip success and return to the normal prompt
for Extender run PORT-010-2026-09-08-04-39-39Z. Its saved P4 capture passes
independent exact-request/single-ACK verification and records 78.094 seconds
of clean observation after success. Together with the physical missing-reply
case, this completes the bounded UARTTEST task. Closed INTEG-004 and removed
it from TODO. Cross-component evidence remains beside Extender's r03 design;
no source or firmware image changed for completion.

## UART flow-control increment authorized

The Author authorized PORT-011 / INTEG-005 at 115200 baud and approved
v0.4.0, uart-flow-probe-r01 and registry r29 as draft identities. Prepare
bounded driver/peer tests and emulator review before candidate deployment.
The mounted SD retains the passing v0.3.0 setup during implementation.

### INTEG-005 draft automated validation

EMOS v0.4.0 draft implements Legacy-only UARTFLOW and private CTS-aware polling
with explicitly claimed/released PC2 RTS. Public UART APIs and UARTTEST retain
their behavior. The configured build, linked checks and runtime regression
passed for agon-emos-v0.4.0-b2026-09-08-05-12-13Z. All 68 host tests and the
same-build ordinary/bad-SD/no-peer checks passed. The new RTS writers are
explicitly admitted with exact register limits; UART-open's original
17-mutation check remains intact. Author graphical acceptance and source freeze
remain pending; no installed image or removable media changed.

## INTEG-005 graphical observation

The Author supplied the same-build v0.4.0 graphical screenshot: SD/CLOCK
PASS, expected CTS did not release timeout, inherited MOS timeout text and
final prompt. The visual check passes; explicit commit approval was requested
before clean candidate preparation. No physical image changed at this point.

## INTEG-005 — Source freeze and deployment approved

The Author explicitly approved committing the reviewed flow-control changes
and proceeding with candidate builds, SD preparation and P4 flashing. Freeze
v0.4.0 / uart-flow-probe-r01 as candidates in the first committed r29 registry.
Only lifecycle metadata changes from the reviewed implementation. Retain both
working EMOS rollback payloads; physical test results remain pending.

## INTEG-005 — Candidate installation media ready

Candidate agon-emos-v0.4.0-b2026-09-08-05-51-31Z passed the complete gate,
68 host tests and exact-image ordinary/bad-SD/no-peer reviews. Firmware is
119863 bytes, SHA256 552f7a22b06e84771e81def293f1127d5278d8fc5f17cefb32187fac881491f8.
Extender prepared and safely unmounted the guarded installation card under
PORT-011-2026-09-08-05-55-02Z, preserving both working rollback images.
The Author's physical Agon installation and UARTFLOW result remain pending.

## INTEG-005 — Installation confirmed; test media prepared

The Author reported successful installation of the frozen v0.4.0 candidate.
Extender confirmed the consumed EMDONE.BIN hash and both rollback payloads,
then staged the exact same-build smoke/UARTFLOW autoexec and safely unmounted
the SD under PORT-011-2026-09-08-06-05-59Z. Both endpoint firmware images
remain unchanged. Physical smoke/flow results are pending; the coordinator
is resolving repeated shortened analyzer captures before the powered-reset
test. No EMOS code change is implicated by the analyzer USB timeout evidence.

## INTEG-005 — Physical EMOS flow checks pass

For PORT-011-2026-09-08-16-16-36Z, the Author confirmed SD/CLOCK PASS,
UART FLOW PASS and normal MOS prompt return. P4's exact stage checker and
the retained waveform also pass; the trace contains both frames, both pauses,
blocked-byte silence and more than five seconds of final quiet. The coordinator
retains the shortened 27.57632/60-second acquisition as a partial result pending
Author disposition. Firmware and candidate identity remain unchanged.

## INTEG-005 — UART flow test accepted and task complete

The Author approved PORT-011-2026-09-08-16-16-36Z as a passed test, accepting
the shortened analyzer capture because the complete exchange and required
quiet tail are present. EMOS SD/CLOCK PASS, UART FLOW PASS and final MOS prompt
are Author-confirmed; P4 and retained-waveform checks also pass. Closed
INTEG-005 and removed its TODO entry. The coordinator preserves the raw
acquisition FAIL and dated acceptance; candidate firmware identities remain
unchanged. No code, build, hardware operation, commit or push occurred here.

## Target-baud UART flow preparation

The Author requested repeating the bounded flow test at 1,152,000 baud and
approved EMOS v0.5.0, uart-flow-probe-r02 and registry r30. PORT-012 coordinates
the test; INTEG-006 owns EMOS changes. Draft review precedes source freeze and
deployment. Preserve passing v0.4.0/r01 images; no hardware result is claimed.

The v0.5.0 draft passes full configured/linked/runtime checks, 68 host tests,
ordinary/bad-SD smoke and bounded no-peer return. The r02 P4 draft builds and
its existing peer/checker tests pass. Prepared graphical review for Author
validation; physical deployment and source freeze remain pending.

The Author confirmed the v0.5.0 graphical SD/CLOCK pass, bounded CTS timeout
and final prompt, and approved source freeze and candidate preparation.
Promote v0.5.0/r02 lifecycle metadata to candidate and build clean committed
inputs; physical target-baud qualification remains pending.

## Target-baud UART flow milestone complete

The Author confirmed final MOS prompt return after all Agon tests passed.
PORT-012-2026-09-08-17-17-51Z is PASS with full acquisition, exact bytes,
both pauses and blocked-send silence. PORT-012 and INTEG-006 are complete
and removed from their authoritative TODOs. Candidate identities are unchanged;
no sustained-load or broader electrical qualification is implied.

The Author approved committing the successful target-baud milestone. For the
next UART procedure, end capture after acquisition completes and five clean
seconds follow P4 final PASS; keep the longer deadline only for stalled runs.
The frozen r02 capture and evidence retain their original 90-second window.

## General Poll increment

Author authorized bounded General Poll over the proven 1152000-baud UART,
and approved EMOS v0.6.0, uart-general-poll-probe-r01 and registry r31.
PORT-013/INTEG-007 own the work; draft review precedes freeze and deployment.
The P4 composition must exercise the retained parser/handler/serializer.

General Poll drafts pass: EMOS full configured/linked/runtime gates, 71 host
tests and same-build smoke/no-peer review; P4 retained-parser compilation and
bounded-adapter tests; 21 capture-checker tests. Prepared graphical review.
Author validation precedes commit, clean candidate builds and deployment.
No physical state changed.

The Author supplied the v0.6.0 graphical review: SD/CLOCK PASS, expected
General Poll transmit timeout without a peer, and final MOS prompt. Recorded
the observation; source-freeze approval remains pending.

The Author approved General Poll source freeze and clean candidate preparation
after the v0.6.0 graphical SD/CLOCK pass, transmit timeout and prompt return.
Promote lifecycle metadata to candidate, commit reviewed inputs and rebuild;
physical General Poll qualification remains pending.

Clean candidate agon-emos-v0.6.0-b2026-09-08-17-50-12Z from e5d9921
and builder cf24304 passes full gates, 71 host tests, ordinary/bad-SD smoke
and bounded no-peer General Poll return. The coordinator prepared guarded
installation media and safely unmounted the SD; working v0.5.0 is preserved.
PORT-013-2026-09-08-17-55-37Z owns preparation evidence. Physical installation
and General Poll qualification remain pending.

## Accepted General Poll milestone — 2026-09-08

The Author explicitly approved the bounded General Poll milestone with the
analyzer exception. PORT-013-2026-09-08-18-20-21Z is accepted PASS for the
exact retained-parser request/reply, framing, flow-control permission and
Agon return to MOS. Acquisition remains FAIL (4.978390/10 seconds), and
waveform quiet tail remains below the five-second requirement (4.655861
seconds). These are accepted exceptions, not repaired or passing checks.
P4 serial monitoring remained clean for 6.005568 seconds after PASS.
PORT-013 and INTEG-007 are complete and removed from their TODOs. No
sustained-load, analog-margin or complete-startup qualification is implied.
Artifact identities and installed firmware remain unchanged.
