# Development log — 2026-09-08

## INTEG-004 — Hardware candidate preparation

The Author authorized the next hardware step after accepting the v0.3.0
graphical review and source checkpoint. Promote only lifecycle metadata to
candidate and build clean inputs through the existing complete gate. Stage
the guarded MOS-only updater, preserve the installed v0.2.0 payload, and keep
the P4 receive-only for the first physical missing-reply test. Both boards
stay powered with the harness seated under the Author's current instruction.
No firmware implementation or emulator launcher changes accompany promotion.

Built and checked candidate agon-emos-v0.3.0-b2026-09-08-04-04-00Z,
117828 bytes, SHA-256 371c1e9182b97d11be3c53fb1133b27bd4e566d9209d0b4c475e92c78561cf36.
The guarded installation card is prepared and unmounted; physical results
remain pending. Extender's design-adjacent preparation record owns the media
evidence and operator handover.

Author reports a good flash; the card's consumed payload matches v0.3.0.
The combined smoke/timeout media is staged and unmounted. Await hardware
smoke and no-peer result; no firmware changes or additional build occurred.

The Author screenshot confirms the exact installed v0.3.0 candidate,
physical SD/clock smoke, intentional missing-reply failure and prompt return.
Extender retains the passing observation beside r03's tests. No calibrated
timeout duration is inferred. The ACK test remains pending; no EMOS source,
build or media change is needed for it.

## INTEG-004 — Complete

The Author confirmed EMOS round-trip success and return to the normal prompt
for Extender run PORT-010-2026-09-08-04-39-39Z. Its saved P4 capture passes
independent exact-request/single-ACK verification and records 78.094 seconds
of clean observation after success. Together with the physical missing-reply
case, this completes the bounded UARTTEST task. Closed INTEG-004 and removed
it from TODO. Cross-component evidence remains beside Extender's r03 design;
no source or firmware image changed for completion.

## UART flow-control increment authorized

The Author authorized PORT-011 / INTEG-005 at 115200 baud and approved
v0.4.0, uart-flow-probe-r01 and registry r29 as draft identities. Prepare
bounded driver/peer tests and emulator review before candidate deployment.
The mounted SD retains the passing v0.3.0 setup during implementation.

### INTEG-005 draft automated validation

EMOS v0.4.0 draft implements Legacy-only UARTFLOW and private CTS-aware polling
with explicitly claimed/released PC2 RTS. Public UART APIs and UARTTEST retain
their behavior. The configured build, linked checks and runtime regression
passed for agon-emos-v0.4.0-b2026-09-08-05-12-13Z. All 68 host tests and the
same-build ordinary/bad-SD/no-peer checks passed. The new RTS writers are
explicitly admitted with exact register limits; UART-open's original
17-mutation check remains intact. Author graphical acceptance and source freeze
remain pending; no installed image or removable media changed.

## INTEG-005 graphical observation

The Author supplied the same-build v0.4.0 graphical screenshot: SD/CLOCK
PASS, expected CTS did not release timeout, inherited MOS timeout text and
final prompt. The visual check passes; explicit commit approval was requested
before clean candidate preparation. No physical image changed at this point.

## INTEG-005 — Source freeze and deployment approved

The Author explicitly approved committing the reviewed flow-control changes
and proceeding with candidate builds, SD preparation and P4 flashing. Freeze
v0.4.0 / uart-flow-probe-r01 as candidates in the first committed r29 registry.
Only lifecycle metadata changes from the reviewed implementation. Retain both
working EMOS rollback payloads; physical test results remain pending.

## INTEG-005 — Candidate installation media ready

Candidate agon-emos-v0.4.0-b2026-09-08-05-51-31Z passed the complete gate,
68 host tests and exact-image ordinary/bad-SD/no-peer reviews. Firmware is
119863 bytes, SHA256 552f7a22b06e84771e81def293f1127d5278d8fc5f17cefb32187fac881491f8.
Extender prepared and safely unmounted the guarded installation card under
PORT-011-2026-09-08-05-55-02Z, preserving both working rollback images.
The Author's physical Agon installation and UARTFLOW result remain pending.

## INTEG-005 — Installation confirmed; test media prepared

The Author reported successful installation of the frozen v0.4.0 candidate.
Extender confirmed the consumed EMDONE.BIN hash and both rollback payloads,
then staged the exact same-build smoke/UARTFLOW autoexec and safely unmounted
the SD under PORT-011-2026-09-08-06-05-59Z. Both endpoint firmware images
remain unchanged. Physical smoke/flow results are pending; the coordinator
is resolving repeated shortened analyzer captures before the powered-reset
test. No EMOS code change is implicated by the analyzer USB timeout evidence.

## INTEG-005 — Physical EMOS flow checks pass

For PORT-011-2026-09-08-16-16-36Z, the Author confirmed SD/CLOCK PASS,
UART FLOW PASS and normal MOS prompt return. P4's exact stage checker and
the retained waveform also pass; the trace contains both frames, both pauses,
blocked-byte silence and more than five seconds of final quiet. The coordinator
retains the shortened 27.57632/60-second acquisition as a partial result pending
Author disposition. Firmware and candidate identity remain unchanged.

## INTEG-005 — UART flow test accepted and task complete

The Author approved PORT-011-2026-09-08-16-16-36Z as a passed test, accepting
the shortened analyzer capture because the complete exchange and required
quiet tail are present. EMOS SD/CLOCK PASS, UART FLOW PASS and final MOS prompt
are Author-confirmed; P4 and retained-waveform checks also pass. Closed
INTEG-005 and removed its TODO entry. The coordinator preserves the raw
acquisition FAIL and dated acceptance; candidate firmware identities remain
unchanged. No code, build, hardware operation, commit or push occurred here.

## Target-baud UART flow preparation

The Author requested repeating the bounded flow test at 1,152,000 baud and
approved EMOS v0.5.0, uart-flow-probe-r02 and registry r30. PORT-012 coordinates
the test; INTEG-006 owns EMOS changes. Draft review precedes source freeze and
deployment. Preserve passing v0.4.0/r01 images; no hardware result is claimed.

The v0.5.0 draft passes full configured/linked/runtime checks, 68 host tests,
ordinary/bad-SD smoke and bounded no-peer return. The r02 P4 draft builds and
its existing peer/checker tests pass. Prepared graphical review for Author
validation; physical deployment and source freeze remain pending.

The Author confirmed the v0.5.0 graphical SD/CLOCK pass, bounded CTS timeout
and final prompt, and approved source freeze and candidate preparation.
Promote v0.5.0/r02 lifecycle metadata to candidate and build clean committed
inputs; physical target-baud qualification remains pending.

## Target-baud UART flow milestone complete

The Author confirmed final MOS prompt return after all Agon tests passed.
PORT-012-2026-09-08-17-17-51Z is PASS with full acquisition, exact bytes,
both pauses and blocked-send silence. PORT-012 and INTEG-006 are complete
and removed from their authoritative TODOs. Candidate identities are unchanged;
no sustained-load or broader electrical qualification is implied.

The Author approved committing the successful target-baud milestone. For the
next UART procedure, end capture after acquisition completes and five clean
seconds follow P4 final PASS; keep the longer deadline only for stalled runs.
The frozen r02 capture and evidence retain their original 90-second window.

## General Poll increment

Author authorized bounded General Poll over the proven 1152000-baud UART,
and approved EMOS v0.6.0, uart-general-poll-probe-r01 and registry r31.
PORT-013/INTEG-007 own the work; draft review precedes freeze and deployment.
The P4 composition must exercise the retained parser/handler/serializer.

General Poll drafts pass: EMOS full configured/linked/runtime gates, 71 host
tests and same-build smoke/no-peer review; P4 retained-parser compilation and
bounded-adapter tests; 21 capture-checker tests. Prepared graphical review.
Author validation precedes commit, clean candidate builds and deployment.
No physical state changed.

The Author supplied the v0.6.0 graphical review: SD/CLOCK PASS, expected
General Poll transmit timeout without a peer, and final MOS prompt. Recorded
the observation; source-freeze approval remains pending.

The Author approved General Poll source freeze and clean candidate preparation
after the v0.6.0 graphical SD/CLOCK pass, transmit timeout and prompt return.
Promote lifecycle metadata to candidate, commit reviewed inputs and rebuild;
physical General Poll qualification remains pending.

Clean candidate agon-emos-v0.6.0-b2026-09-08-17-50-12Z from e5d9921
and builder cf24304 passes full gates, 71 host tests, ordinary/bad-SD smoke
and bounded no-peer General Poll return. The coordinator prepared guarded
installation media and safely unmounted the SD; working v0.5.0 is preserved.
PORT-013-2026-09-08-17-55-37Z owns preparation evidence. Physical installation
and General Poll qualification remain pending.

## Accepted General Poll milestone — 2026-09-08

The Author explicitly approved the bounded General Poll milestone with the
analyzer exception. PORT-013-2026-09-08-18-20-21Z is accepted PASS for the
exact retained-parser request/reply, framing, flow-control permission and
Agon return to MOS. Acquisition remains FAIL (4.978390/10 seconds), and
waveform quiet tail remains below the five-second requirement (4.655861
seconds). These are accepted exceptions, not repaired or passing checks.
P4 serial monitoring remained clean for 6.005568 seconds after PASS.
PORT-013 and INTEG-007 are complete and removed from their TODOs. No
sustained-load, analog-margin or complete-startup qualification is implied.
Artifact identities and installed firmware remain unchanged.

## Visible UART text increment

Author authorized PORT-014/INTEG-008 to connect the proven UART/parser and
browser-display paths, and approved EMOS v0.7.0, uart-visible-text-probe-r01
and registry r32. Keep the diagnostic bounded, Legacy-owned and initially
draft; graphical review precedes source freeze. Existing held work stays held.

Draft EMOS agon-emos-v0.7.0-b2026-09-08-19-16-36Z passes full configured,
linked and runtime checks plus 74 host tests. Same-build stock/EMOS ordinary
and bad-SD smoke and the no-peer CLI diagnostic pass. The new graphical
review is open under agon-extender's .emulator directory; Author review
precedes source freeze and physical preparation.

The Author's screenshot confirms the matching v0.7.0 draft, SD/CLOCK PASS,
expected VDPTEXT transmit timeout and final MOS prompt. Explicit source-freeze
approval remains pending.

The Author then accepted review and explicitly approved committing the changes
and preparing clean candidates. EMOS v0.7.0 is marked candidate; physical
text/browser qualification remains with PORT-014. The Author's separate
Extended Compatible demo work is curated and emulator-tested in
agon-utils/examples/extender.


## SD-loaded text sample and numbering correction

Author approved v0.1.7 as the deliberate next-build numbering reset after
historical v0.7.0, then approved separating the text sample from EMOS. Core
reserves edu.text-probe behind the existing gateway, validates application RAM
and bounded text grammar, and owns UART1/completion/cleanup in Legacy. Old
VDPTEXT retains A6; the application-supplied transaction uses A7. The sample's
snprintf/banner/count live in the coordinating Extender fixture, not in EMOS.
The v0.1.7 draft passes all 74 host tests and configured/linked/runtime gates;
the actual emulated gateway passes invalid-input/no-peer return tests and
same-build SD/CLOCK smoke. Graphical review is pending; no commit or flash.


Author confirmed the initial count preview and requested 250 ms pacing. The
sample now submits one line per gateway call. Successful per-call Core
chatter is suppressed; the application reports final success, and failures
remain visible. v0.1.7 b20-46-22Z passes the full gates and 74 tests again;
the paced sample's graphical review precedes source freeze.


The Author confirmed the paced graphical result and requested deployment to
the mounted SD card. Reviewed implementation is unchanged; v0.1.7/r03 advance
to candidate in registry r34. The required clean-source freeze precedes new
candidate builds, automatic same-build review, then guarded MOS-only install
media. Physical counting and P4 deployment remain separate later steps. No
historical identity or result is relabelled.


## Completed — coordinator hardware evidence

PORT-014 run `PORT-014-2026-09-08-21-22-24Z` passes the SD-loaded application's EMOS gateway path,
11 exact retained-parser request/reply transactions at 1152000 baud, framing
and flow permission. The analyzer retained all 288M samples (12 seconds at
24 MHz), including 6.287879 seconds of final quiet; serial tail is 6.008467
seconds. The Author supplied the rendered banner/count screenshot and confirmed
SD/CLOCK, sample PASS and final MOS prompt for the captured run plus two
additional Agon-only resets. Only the first run is waveform-captured.

Installed EMOS is `agon-emos-v0.1.7-b2026-09-08-20-53-57Z` from clean `026ac46`.
The coordinating sample and P4 receiver are the clean r03 candidates from
`18024d8`. INTEG-008 is complete; the coordinator retains the authoritative
cross-component evidence at
`../agon-extender/hardware/designs/light2-harness-r03/tests/PORT-014-2026-09-08-21-22-24Z/`.
Historical r01/r02 limitations are unchanged. This proves bounded Legacy
qualification traffic; it does not activate Exclusive Compatible or alter
ordinary VDU routing. No EMOS source or artifact identity changed in closeout.


## Browser keyboard becomes the next priority — documentation review

The Author accepted the focused-browser-keyboard critical path and required
close adherence to stock MOS/VDP semantics using only UART for the Agon/P4
traffic. SETUP-005 K001 records browser → P4 processing → stock VDP packets
over r03 UART1 → EMOS-owned key sysvars/keymap/callbacks. P4 does not transmit
MOS RAM structures. Applicable configuration/query traffic uses the same UART.
The onboard clock remains unchanged; no parallel or direct onboard-VDP relay
is required. This does not turn the bounded diagnostic into a committed mode.

The coordinating Extender TODO order is now SETUP-005, REMOTE-001, PORT-006, PORT-005 and PORT-008, each
limited to its keyboard slice. INTEG-009 is the new EMOS implementation owner
and precedes held parallel work there. Architecture, ADR-0013/0014, affected
input/network/transport/ownership tasks, qualification scope and BC-001 now
reflect that direction. The old aware-application forwarding/non-echo profile
is retained as later copied-event scope. F019/R003 retain explicit session
admission/provenance/EMOS authority without changing the stock UART envelope;
R002 retains ordered-event queue obligations. Detailed admission, source and
receiver choices remain open K002/K003 rather than being silently selected.

Official keyboard/API/system-command documentation and the exact AUDIT-004
MOS/VDP sources were reviewed; both upstream checkouts remain clean at their
audited tagged releases. Frozen audit/bench results and artifact registry r34
are unchanged. This pass changes documentation and task ordering only. The
Author requested a pause for review before freeze: no commit, implementation,
build, SD edit, firmware deployment or hardware operation is authorized here.

### EMOS CLI and keyboard command decisions recorded

The Author requested recording the agreed case-insensitive EMOS namespace,
`EMOS EXCOM` / `EMOS LEGACY`, explicit EDU versus routed VDU, persistent
`SET KEYBOARD n` layout and named `EMOS KEYINPUT` sources: mainboard, browser,
and reserved unavailable extender. Bare KEYINPUT reports the source. ADR-0014
and SETUP-005 K004 hold the contract; the architecture and keyboard task owners
now follow it. Interrupt-driven UART1 reception is the accepted direction.
K005 explicitly retains the unresolved conflict between browser input surviving
mode changes and the older inactive-Legacy rule; K006 tracks startup-source
selection separately from layout persistence. No new implementation, artifact,
commit or deployment is authorized; documentation remains under review.

The Author accepted SETUP-005 K005: keyboard selection is independent of
mode changes. `EMOS LEGACY` restores mainboard output and preserves selected
browser input. ADR-0014 and the architecture explicitly amend Legacy's former
total-absence rule for the selected keyboard service; keyboard-only activity
does not imply Dual. Startup-source policy remains K006. Changes remain
uncommitted for review; no code or hardware operation was performed.

The Author accepted autoexec-only persistence for this increment (SETUP-005
K006). Mainboard input is the startup default; `autoexec.txt` restores desired
keyboard source/layout and explicit startup selections. Runtime settings survive
mode changes, but interactive commands create no saved state/.cfg/NVS settings.
A future store is deferred until needed. This clarifies earlier “persistent
layout” wording. The Extender TODO now names the bounded keyboard decision
review rather than implying the whole operating-mode task must finish first.
Documentation remains uncommitted for Author review before freeze.

### Minimal MOS Modules idioms before further EMOS extensions

The Author reversed the defer-and-retrofit sequencing: scope a minimal Modules
foundation now, aiming at upstream consideration while keeping the first work
small. MOS-001 now precedes INTEG-009. Official pinned Modules/Executables docs
and upstream issue 2 plus its ten API-visible comments informed the proposed
Core/transient split: one SD-loaded command provider, permanent interrupt/state
service, bounded CLI/autoexec loading and explicit lifetime/memory checks.
The first proposed proof is read-only command dispatch and slot reuse, before
UART keyboard activation. Dynamic swapping, relocation and full cooperative
services remain later. This concrete split is proposed, not yet accepted;
ADR-0014 records only the accepted strategy. Architecture, task dependencies
and both TODO queues now reflect it. No code, firmware, SD, artifact identity,
commit or upstream submission changed; documentation remains under review.

The Author then rejected the moslet-space Modules direction and explicitly
rejected runtime relocation. The just-proposed SD loader/descriptor foundation
was withdrawn before implementation. Current scope is resident EMOS with
ordinary compile-time linking and clear source-level service/interrupt/command
ownership, without constraining services to moslet RAM. MOS-001 is deferred;
INTEG-009 again leads EMOS implementation. The architecture, ADR-0014 and task
dependencies reflect this correction. No code or commit was made.

### MOS-001 cancelled — resident EMOS extensions selected

The Author explicitly cancelled the MOS Modules development path rather than
leaving it deferred. MOS-001 is closed as cancelled on 2026-09-08 and removed
from the EMOS TODO; the Extender TODO has no open Modules item. Its original
research and proposed work remain historical only. Active task dependencies,
the migration pointer and ADR-0014 now reflect cancellation in favor of
resident, normally linked EMOS extensions. INTEG-009 remains the keyboard
implementation owner. No module loader, moslet-space residency or runtime
relocation is planned. Changes remain uncommitted pending Author review.

### Keyboard and CLI decision freeze authorized

The Author approved freezing the accumulated documentation and requested a
stop after committing, with the first implementation step explained in chat.
The accepted plan covers UART-only browser input, resident EMOS ownership,
case-insensitive CLI and named sources, source independence from display mode,
autoexec-only persistence, single-controller takeover and held-key cleanup,
plus exclusive-mode display notices and initial Legacy return behavior.
MOS-001 is cancelled. SETUP-005's immediate keyboard review is complete and
its broader unfinished scope returns to the Setup queue. Implementation starts
with INTEG-009's resident EMOS command/receiver work when resumed; no source,
firmware identity, build, SD or hardware operation is part of this freeze.

### INTEG-009 Work 1 — contract prepared for review

The Author authorized Work 1 only and requested an emulator attention cue on
completion. The task-local keyboard contract now specifies resident command
behavior, lifetime, UART0 coexistence, readiness/packet boundaries, layout
retention and held-key cleanup, with first supported cases and explicit error
limits. Official keyboard/API/system-variable/VDP documentation and the pinned
MOS/VDP sources were reviewed before EMOS implementation touchpoints.

Two implementation constraints are explicit: current PC2 RTS/nonblocking UART
helpers reject enabled interrupts, and `writeKeyboard` forwards a write-only
setting without retaining layout. The inherited provider/module machinery is
still in EMOS source; KEYINPUT bypasses it and no loader work is reopened.
The draft proposes a stock General Poll ordering barrier, isolated packet
assembly, bounded admission/partial-frame handling and explicit fault recovery.
Those details await Author acceptance; Work 1 is not checked off prematurely.
No firmware code, new identities, SD media or hardware changed. Work 2 remains
unstarted; this document update remains uncommitted for review. The existing
emulator is used only as an attention cue, not new test evidence.

### INTEG-009 Work 1 accepted; resident Work 2 implemented for review

The Author cleared Work 1 and Work 2, then approved EMOS v0.1.8 and registry
r35 as draft identities. The resident command/source service and private UART1
receiver now reuse stock keyboard effects, protect ownership from public UART
calls, preserve UART0 non-keyboard traffic and add bounded VBlank housekeeping.
SET KEYBOARD retains its byte in RAM and follows the selected source. New code
uses normal linking; the cancelled module path is not reopened. The task records
callback/lookup recovery behavior and the linked ABI checks. Host receiver and
real UART-driver tests pass; target build and graphical review are being
completed. Hardware, physical SD and P4 firmware remain unchanged. Nothing is
committed pending the Author's emulator review.

Work 2 automated review completed with build `agon-emos-v0.1.8-b2026-09-09-00-09-53Z`:
75 repository tests, linked guards, stock parity/API/FatFS regression, SD/clock
smoke and repeatable no-peer keyboard-command recovery pass. The graphical
receiver review was launched for the Author. v0.1.8 remains draft; changes are
uncommitted, and no physical SD/Agon/P4 deployment took place. INTEG-009 records
the 3714-byte image and 54-byte BSS increases and the exact validation scope.

The Author supplied the graphical screenshot: smoke SD/CLOCK PASS, expected
absent-peer KEYINPUT timeout, mainboard selection retained and MOS prompt
returned. Work 2 is ready for freeze approval; no commit or next work item was
started on the basis of the image alone.

### INTEG-009 Work 2 source freeze accepted

The Author explicitly requested “freeze it and tell me what's next” after the
expected graphical result. Work 2 is complete and its reviewed source/tests,
accepted Work 1 contract and coordinating registry/task records are frozen in
local commits. The 75 passing repository tests and target/emulator checks apply
to the unchanged implementation. The reviewed v0.1.8 build retains its draft
identity; no physical qualification, deployment or push is part of this action.
INTEG-009 Work 3 is next: prove ordinary MOS key retrieval/editor, sysvars/count,
virtual map and callbacks using controlled stock keyboard packets, including
applicable source-aware keyboard configuration/query routing. Work 3 has not
started; full browser focus/session work remains with the coordinating tasks.


## INTEG-009 Work 3 — public keyboard API exerciser

Author authorized Work 3 and approved keyboard-api-probe-r01 / registry r36.
The SD-loaded C exerciser and small assembly callback use public MOS APIs;
EMOS v0.1.8's reviewed binary remains unchanged. A controlled host peer feeds
stock packets into the emulated UART1. Its actual ISR/parser/assembly path
passes callback order/edits/registers, sysvars/modifiers/map, count wrap,
KEYSTATE, getkey, line editing, callback removal, locale retention and source
return. Existing repository tests: 75 passed.

Fab's missing UART1 RX interrupt is handled by a separate, generic test-runtime
adaptation in mos-agondev TEST-001. No reference checkout or independent Fab
fork was edited. Test pacing allows the CLI fake VDP to answer its mode query
before injecting the final command. The raw VDU keyboard controls remain an
explicit integration boundary in the task, separate from the tested named
Keyboard setter and status packet effects. Await human graphical review and
explicit source-freeze approval; no physical deployment occurred.

The final graphical run also passed, ending at the mainboard prompt. In
addition to UART1 RX, the isolated peer needed explicit ownership of PC3 CTS
because graphical Fab's joystick-high input otherwise masks its ready signal.
The agent observed the screen; Author acceptance remains pending. The fixture
build is keyboard-api-probe-r01-b2026-09-09-01-03-19Z.

The Author explicitly confirmed that the Work 3 emulator review passes.
Record the bounded proof as accepted while preserving W3-K001 as open.
Source-freeze approval and authorization to begin Work 4 remain pending.

The Author subsequently authorized the Work 3 source freeze and bounded Work 4
held-key cleanup, truncated-packet timeout and explicit recovery tests. Reviewed
fixture and tooling hashes still match the accepted manifests; the EMOS binary
remains unchanged. Work 4 will receive a separate emulator review before commit.


## INTEG-009 Work 4 — recovery fixture prepared

After the accepted Work 3 freeze, the SD exerciser and peer were extended for
held-key/repeat cleanup, stale admission, idle, whole-frame partial timeout,
latched fault and explicit retry. Firmware and runtime remain unchanged.
Python syntax and diff checks pass; fixture revision approval, build, paired
execution and human review are pending. No deployment or new qualification.


Author approved keyboard-api-probe-r02 / registry r37. Build
`keyboard-api-probe-r02-b2026-09-09-01-54-03Z` passes all nineteen paired
emulator stages and mainboard command return. Held-key cleanup, lock retention,
stale admission, idle, partial-frame expiry, fault latching and explicit retry
all pass through the actual resident receiver. The firmware/runtime hashes are
unchanged. Input hashes and diff checks pass; graphical review is ready.
Work 4 remains uncommitted and its wider physical/session cases remain open.

The generated graphical profile was launched only after the automated results
and preparation were complete. Its peer reports all nineteen stages PASS and
the session exited cleanly; Author acceptance/commit approval remains pending.


The Author supplied the Work 4 r02 emulator screenshot. It confirms all four
recovery PASS lines, the intended transient browser fault, successful explicit
retry, final API PASS and mainboard MOS prompt. Record the bounded graphical
proof as passed; source-freeze approval and wider physical/session work remain
pending. No new build, commit, deployment or emulator launch was required.


## INTEG-009 bounded Work 4 source freeze

The Author requested freezing the reviewed recovery checkpoint. The r02 fixture,
controlled peer, immutable profile-peer preparation and coordinating r37 records
are committed without changing the reviewed behavior. All nineteen paired and
graphical stages pass; the supplied screenshot confirms successful retry and
mainboard prompt return. Maintained input hashes still match the review bundle.
No firmware rebuild, candidate promotion, push or physical deployment is part
of this freeze. Work 4's remaining P4/session/hardware obligations stay open.


## INTEG-009 — controlled P4 sender observer

The Author authorized the coordinator's P4 sender increment and approved
uart-keyboard-probe-r01/registry r38. Added an ordinary KBWIRE SD application
using public callbacks, sysvars, keymap and source-selection CLI. It checks
twelve controlled events and restores mainboard input with a finite deadline.
The EMOS v0.1.8 firmware and UART1 emulator runtime remain byte-identical.

Draft uart-keyboard-probe-r01-b2026-09-09-02-36-43Z passes both the twelve
serializer-produced packets and the admitted-but-silent input deadline, each
with mainboard cleanup and a subsequent normal MOS command/prompt. The review
profile pins sender host bytes/provenance plus firmware, runtime, fixture and
peer inputs. Graphical review and explicit freeze approval remain pending;
no physical SD, device, installed firmware or official reference changed.


The Author supplied the matching r01 graphical screenshot: EMOS v0.1.8,
SD/CLOCK PASS, twelve-packet/callback/counter PASS, held-key/map/modifier/repeat
PASS, restored mainboard input and final MOS prompt. The controlled peer's
record also reports twelve keys and PASS. The bounded graphical result is
confirmed. Source-freeze approval and physical qualification remain pending;
no new build, emulator launch, commit or hardware operation was required.


## Controlled keyboard checkpoint frozen

The Author requested source freeze after supplying the matching graphical PASS screenshot, then authorized continuing to paired hardware preparation. Reviewed sender, SD observer, peer and build-tool inputs still match their manifests. Freeze the bounded r01/r38 implementation without changing its reviewed draft binaries. Hardware qualification and browser integration remain open. No push or physical operation is part of this checkpoint.
