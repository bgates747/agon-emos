# PORT-200 development log — 2026-08-24

## PORT-200A — plan and isolation checkpoint

The Author authorized an unattended goal-mode run of the PORT-200 backlog while
another agent may continue Extender work. Before implementation or test-source
changes, two isolated worktrees were created:

- `mos-agondev` branch `dev/port-200`, rooted at published EMOS qualification
  commit `b12fcaba97b1a9d43a5b2e247cc96846febac934`, in the isolated
  `mos-agondev-port200` worktree;
- `agon-mos` branch `dev/port-200`, rooted at published maintained-source commit
  `a8dc891a3668660978fa66d54b3d7638ecc876e2`, in the isolated
  `agon-mos-port200` worktree.

The original `dev/emos` worktrees were clean before this plan was written.
`agon-extender` is a read-only architectural input and is outside the write
scope. `TODO.md` contains the numbered execution plan, success boundaries, and
explicit non-goals. PORT-203B is deliberately a real-hardware gate: local work
may make it turnkey but cannot manufacture its completion evidence.

The frozen input inventory completed before implementation or test-source
changes:

- AgonDev is the clean `nightly` commit
  `b67ab2444a63267a42193f204889d466765d8dd2`; the public `master` head still
  resolves to that commit.
- The maintained isolated MOS source is clean at `a8dc891`; the official
  v3.0.2 base is `8336409351ee5314e02801a7b72a4f1bb5282519`.
- The canonical Fab checkout is tag 1.2.3 commit
  `fbb7d7ca887a06966ca8a62ed22272409a5ab640`, which is also the public `main`
  head. Its tracked `sdcard` submodule is already dirty, so it is a runtime
  input rather than clean-source evidence. The executable is 1,896,928 bytes
  with SHA-256 `2d1b290c37ea37cb7044f24522522f546b750b7ef93050f1ebbdbd1c66e30ac3`.
- Fab's stock Platform MOS and map exactly match the official v3.0.2 release:
  108,490-byte MOS SHA-256
  `d564243283972690933a4554296ad6202ca4ef54572279533a942960846bebae`
  and 167,185-byte map SHA-256
  `d69e60bbce61a7b4b3eef318ba395f11c4e1a5b585755755113992dc94edcb86`.
  The 10,279,496-byte stock VDP module SHA-256 is
  `cfd0aad2108e074af207f5bc6f1b73976097851405bc7c3f8949c7a165df2cb3`.
- The official release's MOS.hex SHA-256 is
  `821ffd6595ca3293a72c4cd5c4c0025bc41ebae84a6eb293a13d0dbf0d6912c4`.
- The documentation input is clean at
  `f9806bd3cbff6ed5d1c08bef1d51fed11764b86b`.
- The prior-art repository `tomm/rainbow-mos` is pinned from public `main` at
  `21d13d45f5546f23a057c132f774be4dfbee3a49` (2026-07-31). Its MIT license
  file SHA-256 is
  `fae2cacdc47dc5ed8efbd052201834108167942f0b524b8238327e9fbe13fd25`.
- The former baseline-only MOS source commit `5f67b1ca...` is not present in
  the current maintained-source object database. PORT-205 must therefore
  resolve public provenance rather than assuming that hash is locally
  obtainable.
- The Extender checkout remains at frozen commit `10f2eb2`; another agent has
  active dirty work under `docs/tasks/PORT-003*`. This goal will not inspect as
  generated evidence, modify, stage, or commit that concurrent state.

The isolated harness was then configured with relative ignored links to the
pinned AgonDev release and isolated maintained-source worktree, prepared all 69
tracked MOS inputs, and generated its own emulator profile from Fab 1.2.3.
PORT-200A is complete. Both published `dev/emos` worktrees remained clean.

## PORT-201 — expanded emulator parity

PORT-201A and PORT-201B are complete. The complete matrix and observation
boundaries are frozen in `docs/port-200-qualification.md`. The linked-ROM VDP
interpreter now executes idle bytes, corrupt-state recovery, every legal packet
length 0 through 16, partial bodies, back-to-back recovery, all 239 oversized
lengths, and a bounded per-byte instruction count. The existing handshake and
stock-image negative control remain intact.

The target contract now uses separate temporary hostfs trees and four cold
boots: candidate create/verify followed by stock ZDS create/verify. It proves
MOS handle writes and high-level save/load/copy/rename/delete plus direct FatFS
directory/chdir/write/put/close/rename/stat/read/unlink behavior. The first boot
must leave one exact `PORT-201-PERSIST` payload; the second must read it and
restore the byte-for-byte initial snapshot. Both images passed. Fab CLI cannot
prove RTC-set persistence, physical UART errors, or electrical timing; those
cells are explicitly transferred to PORT-203 rather than counted as emulator
passes. Fab's absent `_f_sync` interception, rebooting `_f_truncate` path, and
`f_putc` BC-return limitation were reproduced and bounded.

## PORT-202 — public ABI matrix

PORT-202A and PORT-202B are complete within the safe unattended boundary. The
inventory covers 127 high-level jump slots, 38 FatFS slots, 33 ADL function
slots, 128 public header declarations, 123 wrapper sources, and all 126 IX
operands. The executable MOSlet now has 86 named assertions spanning pure,
signed/zero-extended/high-byte, pointer, read, write, state, path, error, and
cold-boot persistence behavior. Its link verifier requires every exercised
archive member. Callback/vector, raw SD, I2C/UART hardware, RTC set, volume
label, mount/format and emulator-unsupported entries are explicit unrun cells.

The unchanged `mos_extractnumber("12345")` call passes candidate and reference
in its minimized noinline frame. The earlier monolithic-frame-only failure has
no matching wrapper defect in the source-wide slot audit; the safe split and
residual compiler/probe limitation are now frozen rather than misattributed.

## PORT-204 — upstream defect decisions

PORT-204 is complete for the current pins. Public AgonDev `master` remains the
pinned `b67ab244...`; Fab public `main` and tag 1.2.3 remain `fbb7d7c...`.
All three wrapper defects and Fab's `_poke24` object-size defect remain.
Accordingly the three minimal project wrappers and bounded large-file workaround
remain; the structural failing shapes and runtime passing paths were rerun.

## PORT-205 — publicly reproducible baseline

PORT-205 is complete. Public author-fork commit `a8dc891...` replaces the
unreachable historical `5f67b1ca...` identity in baseline schema 2. The official
parent, AgonDev, Fab, documentation, stock MOS/map and VDP identities are all
public and hash-bound. Locally compiled Fab bytes are profile-bound rather than
mistaken for a reproducible public artifact. Clean-checkout tests no longer
depend on a sibling directory name, current `HEAD`, or stale ignored conversion
output. The refreshed public-input audit matches `evidence/baseline.json`.

## PORT-207 — Rainbow MOS prior art

PORT-207 is complete. Public MIT commit `21d13d4...` was reviewed and built with
the pinned AgonDev compiler. Its 83,380-byte result independently demonstrates
a compact direct AgonDev MOS. Lineage, source model, GNU assembly, linker/startup,
`-Oz` and FatFS register-pressure workaround, runtime, ABI additions, size and
evidence were classified in `docs/prior-art-rainbow-mos.md`. No code was
imported; durable credit and the build/size comparison are the adopted results.

## PORT-203A — physical gate preparation

PORT-203A is complete. `docs/port-203-hardware.md` defines preparation,
rollback, seven ordered stages, abort conditions, thresholds, raw capture
requirements and the exact validation command. The new validator rejects
missing/reordered/failed stages, synthetic approval, firmware mismatch,
symlinks/path escapes and artifact hash drift. No physical result was created;
PORT-203B remains an author-operated release gate, not an unfinished agent
implementation task; its procedure is maintained outside `TODO.md`.

## PORT-200B — final local qualification

PORT-200B is complete. The full `baseline-check` executed unit tests,
environment validation, prepared-source provenance, toolchain/C/assembly/linker
and restricted-runtime checks, the 113,053-byte firmware build, linked EMOS ABI
and VDU checks, provider media/runtime, custom boot, differential shell parity,
expanded linked-byte VDP regressions, and the four-boot writable ABI contract.
The public-input audit matched baseline schema 2. Its first tail pass exposed
that `verify_probe_outputs.py` still consumes the deterministic 611-byte
firmware-probe hash; schema 2 was corrected to retain deterministic probe
measurements while continuing to exclude only the path-dependent Fab
executable. The audit, 17-object/98,972-byte C probe, and pinned ZDS negative
control then passed.

The initial plan checkpoint is `c5f0c04`; the locally complete qualification
milestone is `7a526f0`. Both were pushed to `origin/dev/port-200`. The isolated
maintained-source branch remains clean at `a8dc891`; it required no new source
change. Both published `dev/emos` worktrees remain clean, and the concurrent
Extender PORT-003 files remain untouched. No actionable TODO remains. Physical
qualification is still unperformed, so the image is an emulator-qualified
release candidate, not a hardware-qualified release.
