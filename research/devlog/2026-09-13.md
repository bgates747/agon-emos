# Development record — 2026-09-13 UTC

## INTEG-013 accepted: owned mainboard SD service

INTEG-013 is complete within Extender PORT-017's declared SD-service scope and
is removed from the unfinished TODO. The Author confirmed the physical flash
and visual startup, then replied "keys worked fine" to the fresh live-transfer
Escape/typed `RUN . /` check. The EMOS-owned transport preserves native Extender
keyboard input while the foreground application performs filesystem work.

The exact deployed clean source is
`71f362af4c64c8d2b74dd532f352358200a78f5c`:

| Artifact | Build | Bytes | SHA-256 |
| --- | --- | ---: | --- |
| EMOS | agon-emos-v0.1.14-b2026-09-13-01-07-47Z | 130919 | eea1a479516df733688803f775d65c41a7c48a546927c7f07c0a489f8cd09808 |
| Service | sdserve-v0.1.0-b2026-09-13-01-07-48Z | 18974 | 8903bb934f26af914f31d92a92508bc5095d09f4c2fa027c7b05155a9544a317 |

The paired P4 image is
`uart-excom-console-r12-b2026-09-13-01-07-48Z`, SHA-256
`ff1e0da79a844eed4a2a4c7bce3d346750610409f8a4d161129c3b599b26c7e0`,
from agon-extender `69da46ac1795bbab748fefed22f9d73ffc96afcc`.
EMOS has 153 ROM bytes spare; Core static RAM ends at BDAAA below BF800. Native
three-byte ADL loads reduced gateway decoding cost without dropping supported
features; host decoding remains bytewise. Full wrapper/link/runtime guards and
89 EMOS host checks pass. No new build was made for this documentation closeout.

Ten consecutive physical staged uploads, complete readbacks, verified
activations and previous-version checks pass for sizes from zero through
131731 bytes without card movement or processor reset. The largest full cycle
took 360.381 seconds, including multiple complete reads and durable host audits;
that is not an upload-only timing. Physical malformed request, CRC, busy,
sequence, retry, cancellation and explicit recovery checks also pass. One
successful HTTP response was intentionally withheld before durable host
acknowledgement; no physical UART disconnect is claimed.

The fresh keyboard observer retained all 45368 acknowledged bytes as an exact
prefix, preserved the old target, then recovered the orphan and completed a new
verified upload. Human confirmation is stored separately from the immutable
machine result. An earlier reflexive reset after an expired observer is a
separate event, not part of that fresh pass or the ten uninterrupted cycles.

Raw-FAT headless disk exhaustion retained a 26624-byte partial file, reported
FR_DENIED, preserved the old target and permitted recovery. Host engine tests
inject write, sync, close, read, rename and unlink faults. These establish
software behavior, not induced physical-media failure or power-loss atomicity.
Extender owns the exact run manifest and composed acceptance at
`docs/qualification/mainboard-sd/2026-09-13.md`.

After the native CLI restart closed the original MOS batch, network delivery
updated `/autoexec.txt` to run the service with `/` scope and retained its old
version as `.p17bak`. That final startup edit was not rebooted again; its exact
service command already passed via native input. Full physical readback of
Rally's 131731-byte binary matched its preserved SHA-256
`e14e5c0f155a4f035e925b1da5971e8f7202fe47c7147e24a5726ef937d4fe5f`.
No game/track files or onboard VDP were changed by this task.

The [gateway contract](../../docs/emos-v1-contract.md) and
[service guide](../../projects/sdserve/README.md) now describe maintained
behavior; Extender's operating/wire/procedure documents have role-named homes.
EMOS v0.1.13 rollback and host commissioning backups remain preserved. Registry
candidate identities are unchanged; scoped acceptance is not general release.
Rally and the service occupy the foreground separately. Remote game launch and
automatic hard-hang reset are absent. No downstream task or push was started.
