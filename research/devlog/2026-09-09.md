# 2026-09-09

## INTEG-009 shared UART typing review

The Author preapproved versioning for focused browser typing. Draft v0.1.9
extends the existing resident receiver with ordered text acknowledgements;
keyboard effects continue under IRQ while the foreground sends. The new
browser-keyboard-probe-r01 SD application uses public keyboard callbacks and
the resident text gateway. It never configures transport, switches video mode,
or activates ExCom routing. The bounded implementation record is in INTEG-009.

The repository build wrapper passes 134 builder tests, 75 EMOS tests and all
linked guards. Mapped keys emitted by the retained P4 serializer drive the real
emulated EMOS through an edited text round trip; missing acknowledgement also
returns boundedly to mainboard/MOS. Human graphical review remains pending.
No firmware installation or commit has been performed.

## Graphical result and candidate freeze

The Author's screenshot confirms the edited `aB3?` / `z` round trip, eight
edited characters, PASS, mainboard input and MOS return. Under the standing
preapproval, freeze the implementation and candidate identities in registry
r41 before building hardware artifacts. Physical typing remains unqualified.
