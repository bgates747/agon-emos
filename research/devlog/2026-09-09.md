# 2026-09-09

## INTEG-009 shared UART typing review

The Author preapproved versioning for focused browser typing. Draft v0.1.9
extends the existing resident receiver with ordered text acknowledgements;
keyboard effects continue under IRQ while the foreground sends. The new
browser-keyboard-probe-r01 SD application uses public keyboard callbacks and
the resident text gateway. It never configures transport, switches video mode,
or activates ExCom routing. The bounded implementation record is in INTEG-009.

The repository build wrapper passes 134 builder tests, 75 EMOS tests and all
linked guards. Mapped keys emitted by the retained P4 serializer drive the real
emulated EMOS through an edited text round trip; missing acknowledgement also
returns boundedly to mainboard/MOS. Human graphical review remains pending.
No firmware installation or commit has been performed.

## Graphical result and candidate freeze

The Author's screenshot confirms the edited `aB3?` / `z` round trip, eight
edited characters, PASS, mainboard input and MOS return. Under the standing
preapproval, freeze the implementation and candidate identities in registry
r41 before building hardware artifacts. Physical typing remains unqualified.

## First physical browser typing checkpoint

Author reports visible browser typing and explicitly confirms Enter/Backspace.
Latency and apparent focus/capture loss remain unresolved; disconnection is a
hypothesis. Coordinator REMOTE-001 owns the next bounded diagnosis. Physical
Escape/MOS return and the result byte are not confirmed. Record progress and
stop as requested; no code, artifact status or hardware changes.


## Native USB source increment — PORT-015 W3

Author authorized the ordinary-CLI USB increment on 2026-09-09. Draft
agon-emos-v0.1.10 reuses resident UART1 framing/interrupt effects for the
explicit extender selector. A matched readiness poll commits the requested
source; timeout retains mainboard. Fault cleanup covers either UART1 source.
Mainboard display/clock and the browser-only text gateway are unchanged.

The paired native P4 composition provides only USB acquisition; the browser
composition provides browser acquisition. General Poll remains stock and does
not authenticate firmware/provider identity. Select the documented matching
images. Direct browser/extender cross-selection returns BUSY without touching
the current receiver; select mainboard first until P4 provider switching is
implemented. No new source-selection packet or simultaneous input mixing.

Native SET KEYBOARD additionally waits for an ordered stock poll after locale.
The first P4 composition supports UK/US; other layouts withhold readiness and
cause a bounded fault rather than silently mapping as US. Wider locale,
settings/query/LED and VDP-local control behavior remain unqualified. Host
checks cover extender admission, no-peer rollback, mainboard exclusion,
malformed-frame cleanup, explicit retry and source return. Emulator review,
source freeze and hardware deployment remain separate gates.

W3 headless ordinary-CLI check passes with the draft built as
agon-emos-v0.1.10-b2026-09-09-21-25-49Z: 258 native USB mapped, retained P4
serializer packets exercise actual MOS Backspace/arrow editing, shifted text,
source reporting and mainboard prompt return. Withheld readiness retains
mainboard and returns to its usable prompt. No SD typing application was added.
The build wrapper's qualification and linked ABI/baud/IRQ/public-UART guards
pass; the receiver host test additionally checks native failed-layout cleanup
and retention of the prior layout. Flash image is 127063 bytes (4009 bytes
within the 128 KiB limit remain); static internal RAM ends at 0x0BD7B2,
6066 bytes above 0x0BC000. The remaining 10318 bytes before the stack origin
are shared runtime headroom, not a measured stack/heap high-water guarantee.
Source remains draft and uncommitted pending the Author's graphical review.


Author supplied the passing graphical CLI screenshot, accepted the result and
authorized hardware preparation. Freeze v0.1.10 as candidate with unchanged
executable logic. Build through the guarded wrapper before SD staging; preserve
the installed v0.1.9 rollback. Physical installation/input proof remain pending.

## INTEG-010 ordinary ExCom console software draft

The Author authorized bounded idle-CLI Legacy/ExCom switches without reboot,
with fresh destination displays and keyboard source/layout preserved. Added
resident compatible-UART dispatch and control to the existing coordinator,
private staged stock display-reply publication, and explicit mainboard notices.
UART1 ownership now survives keyboard-source release while ExCom needs it.
The change retains the stock ISR handlers and their private UART0 parser state.

Draft agon-emos-v0.1.11-b2026-09-10-00-50-45Z passes the qualification wrapper,
76 product tests, runtime and profile-owned linked guards. Actual EMOS passes
two headless ExCom/Legacy cycles and withheld activation with subsequent Legacy
input, using Extender's native stock-VDP review peer. Human visual review and
paired P4 hardware proof remain pending. Installed candidate/rollback images
and SD contents remain unchanged. New source is uncommitted for that review.

## ExCom mainboard visual review accepted; checkpoint freeze authorized

The Author supplied the final emulator screen: Legacy notice, ordinary echo
output, Extender keyboard source and restored MOS prompt/cursor. The Author
then requested freezing and continuing. Freeze the reviewed implementation and
its bounded evidence; N001 reference rendering and paired physical qualification
remain open. Candidate preparation uses existing version preapproval. The
Author holds the SD for independent testing; no SD or running-board changes
accompany this freeze.

Promoted the reviewed v0.1.11 identity to candidate under the Author's standing
preapproval and explicit freeze-and-continue instruction. No executable logic
changes follow checkpoint 8e63cc4. Candidate rebuild and local installer
preparation precede any later SD or physical-board deployment.
