# Development log — 2026-09-07

## QUAL-002 — Minimal EMOS deployment plan

At the Author's request, created [QUAL-002](../../docs/tasks/QUAL-002.md) and
added it to the authoritative TODO. The immediate milestone is an identified
EMOS Legacy boot using onboard VDP UART0 and VSync, automatic SD-program
execution, clock progress and prompt return. Exclusive Compatible remains
the first complete Extender mode to prove in a subsequent increment.

The Author explicitly recalled the first EMOS image leaving the Agon unable
to boot. The plan carries forward QUAL-001's measured UART-divisor failure,
the committed widening correction, the mandatory final linked-image guard,
and Fab's inability to detect the physical baud mismatch. Independent
keyboardless stock-MOS recovery must be demonstrated before candidate flash;
SD autoexec alone cannot recover EMOS that never reaches startup completion.

The current unversioned identity and previous rejected candidate remain
non-deployable. The plan requires a reviewed identity, clean candidate inputs,
the relevant existing checks and explicit deployment authorization. It also
replaces the pin-driving pinwalk autoexec for the eventual boot test and keeps
broader physical qualification and parallel work in their existing tasks.

Documentation only: no source, build, emulator, media, hardware, identity,
commit or push operation was performed.

## QUAL-002 W1 — Source review and recovery prerequisite

The Author approved W1 as one bounded investigation. Recorded the selected
EMOS/build/toolchain revisions and traced reset, Port C initialization, onboard
UART0 startup, semantic VDU routing, Legacy initialization and autoexec ordering
in the [task findings](../../docs/tasks/QUAL-002.md#w1-findings--2026-09-07).
The earlier UART widening fix remains; its source-only oracle reports divisor
1. No target build or linked-image check was performed. No transport logic
change is currently indicated; W2 needs candidate identity/boot diagnostics
and a controlled automatic smoke, with existing checks retained.

Verified the official MOS v3.0.2 recovery binary against the published digest
and the generic builder's retained reference. Found that the previously used
P4 ZDI tool is explicitly retired and embeds corrective EMOS, not this stock
payload. The preferred upstream recovery method uses a separate supported
ESP32 and its host serial menu; spare-board availability is pending. The
current P4 is visible on the Pi, but USB discovery does not establish a ready
ZDI programmer. Recovery selection and the proposed v0.2.0 identity remain
open; W2 has not started. Local reference copies and bench details are ignored.
Official checkouts remain clean. Documentation/reference inspection only;
no build, media or hardware mutation, commit or push occurred.

The Author subsequently reported an available WROOM development board. Recorded
it as the proposed external recovery programmer, pending exact module, flash
capacity and GPIO26/27 verification against the pinned upstream tool. This
resolves spare-board availability, not programmer readiness. No image was
prepared and no hardware action was performed.

Following the Author's legacy-documentation pointer, inspected the clean frozen
WROOM reference at `c200d97fac4027da386c24aecba1f367dd74d329`. Its board record
identifies the ESP32-WROOM-32D/38-pin DevKitC, measured 4 MB flash and exposed
GPIO26/27; its existing code/configuration uses `esp32dev`. These records
resolve the board facts already known to the project. Selected that board for
upstream external recovery and closed W1 item 2's selection work. Current
attachment checks and demonstrated recovery remain later execution gates.
No legacy code was imported or changed. Candidate identity approval remains
open; W2 has not started.

The Author accepted the proposed identity and clarified that the WROOM must
remain unwired and its firmware untouched unless recovery becomes necessary.
Updated the active plan to supersede its earlier pre-deployment recovery
rehearsal requirement. W1 is complete; W2 will prepare/check the ordinary EMOS
candidate, boot diagnostic, automatic smoke and keyboardless stock-MOS update
path. The selected stock image and upstream WROOM method remain contingencies.
Mandatory candidate/link checks remain; no WROOM build or wiring preparation,
EMOS build, deployment or commit was performed.

## QUAL-002 W2 — Identified ordinary boot prepared for review

The Author authorized preparation. Stamped the approved v0.2.0 draft identity
and shared its exact build diagnostic between normal boot and EMOS STATUS.
Added the ordinary SD-read/clock-progress smoke, identified build-bundle helper
and isolated stock/EMOS review profiles. No transport implementation changed.
The existing runtime regression now derives expected identity from the hashed
firmware manifest. The ordinary boot media contains no provider/fake-mode work.

Resolved two build integration issues without waiving checks: the generic
builder requires its own Python environment, and its stock shell comparator
needed an explicit exact boot-identity addition from the EMOS source profile.
The generic change stays in mos-agondev under BUILD-001. Removed an unnecessary
external MODE utility from the minimal smoke script after the stock test
exposed that missing dependency. Preserved diagnostic logs for failed attempts.

Review build `agon-emos-v0.2.0-b2026-09-08-00-22-20Z` passed the complete
configured build gate, 66 EMOS tests, registered linked checks (UART0/UART1
divisor 1), ABI/VDU checks, provider validation and the existing EMOS runtime
transcript. Identical ordinary smoke bytes pass under official MOS v3.0.2 and
EMOS, and bad-SD controls fail as intended. Isolated launcher integrity checks
reject changed firmware or boot media. Hashes/logs and exact evidence limits
are recorded in QUAL-002; graphical Author review remains pending.

The Author clarified agon-flash's keyboardless switch. Verified unmodified
v1.9 release bytes and pinned source: MOS-only `-f`, programmed-flash CRC and
automatic reset. The separate draft test sheet reuses rename-before-flash to
stop repeated installation, followed by a powered-off SD handover to the smoke.
Stock/EMOS control-flow checks with ECHO substituted for FLASH confirm the
guard; no flashing was attempted. The actual physical media backup/staging,
candidate freeze and W3 deployment remain pending. WROOM, Agon, P4, SD card
and official reference checkouts remain untouched. No commit or push occurred.

The Author then supplied the graphical emulator screenshot for build
`agon-emos-v0.2.0-b2026-09-08-00-22-20Z`. It shows the selected VDP/MOS/EMOS
identities, SD and CLOCK PASS, final smoke PASS, unchanged Legacy/inactive-EDU
state and the MOS prompt. Recorded the graphical check as PASS in QUAL-002.
The identity wraps but remains readable. This is conversation-supplied emulator
evidence; no physical result is inferred. Commit approval and clean candidate
freeze remain pending. Documentation only; no image, media or hardware change.

The Author explicitly accepted the emulator pass and authorized committing
the reviewed checkpoint in EMOS, mos-agondev and Extender. Recorded that
acceptance in QUAL-002 and TODO. The checkpoint preserves v0.2.0 at draft
status, the reviewed ordinary boot implementation and scripts, and the exact
dirty-image evidence. Clean deployment-candidate freeze and physical approval
remain later gates; this authorization does not select the dirty review image
for flashing or require a WROOM operation.

## QUAL-002 — Physical stock preflight prepared

The Author pushed the checkpoint and made the SD card available for EMOS
installation preparation. The accepted test sheet first requires an ordinary
stock-MOS smoke. Rebuilt clean committed EMOS/build-tool inputs as
`agon-emos-v0.2.0-b2026-09-08-00-46-20Z`; all configured gates and positive/
negative stock/EMOS emulator smokes pass, including linked UART divisor 1.
The committed source still labels firmware draft, so no EMOS image was staged
for flash.

Preserved the card's pinwalk autoexec and existing firmware files outside the
card, verified its stock MOS/flash-utility hashes and absence of overriding
boot scripts, then staged the ordinary stock smoke and LOAD/RUN autoexec.
Synced and unmounted successfully. The detailed preparation record lives
beside the boot test sheet. Physical observation and operator confirmation of
powered-off r03 harness isolation remain pending. No processor firmware, WROOM,
reset circuit or power state was changed by the agent. Documentation changes
remain uncommitted.

The Author corrected the video-mode setup convention: only autoexec selects
the mode, through the built-in MOS `VDU 22 n` command. Persisted this in the
agent instructions, updated the draft test sheet and future media generator
to use `VDU 22 3`, and retained the mode-independent EMBOOT program. Official
MOS documentation confirms the space-separated syntax; the current VDP table
defines mode 3 as 640×240, 64 colours, 60 Hz. The earlier external MODE utility
was unnecessary, but omitting mode setup entirely was not the desired final
configuration. Existing review media, binary hashes and the already unmounted
card remain their original evidence; the new line applies at the next staging.
No build, commit or physical operation accompanied this correction.

The Author remounted the Agon card for that update. At 01:01:54 UTC on
2026-09-08, preserved its existing LOAD/RUN autoexec and prepended `VDU 22 3`.
Verified the new CRLF script, synced and unmounted successfully. Recorded its
hash and backup beside the stock-preflight evidence. Only autoexec changed
on the card; the ordinary smoke still runs under installed stock MOS and no
flash command was staged. Physical boot evidence remains pending.

## QUAL-002 — Stock hardware pass and proposed candidate freeze

The Author reported all requested hardware stock-smoke checks passed and
remounted the card. Recorded the operator-reported pass beside the test sheet;
the card still matches the documented mode-3 autoexec and smoke hashes. No
new raw screenshot or firmware-version transcript accompanied the report.
Preserved another backup of that passed media before installation preparation.

Prepared the narrow candidate-freeze proposal in Q003: v0.2.0 candidate status,
the current test sheet identified as emos-ordinary-boot-r01, registry r22, and
a helper that accepts candidate status only with clean committed EMOS and
builder inputs that remain unchanged throughout the build. Verified that the
dirty candidate invocation refuses before creating output or preparing source;
the registry proposal and diff checks pass. No C smoke, UART, transport or VDU
logic changes are proposed. Requested the Author's required identity/freeze
approval before committing, building and staging the one-shot MOS flash.

The Author approved versioning and the prepared freeze. Marked Q003 accepted
and the procedure ready for candidate use. The source freeze retains unchanged
MOS/transport/smoke-program behavior, with candidate diagnostics and mode 3 in
autoexec. Commit the controlled inputs, then build/check from their clean
commits and prepare the one-shot MOS flash under the existing authorization.
The procedure identity should have been settled with the earlier checkpoint;
leaving it outstanding delayed the handover unnecessarily.
