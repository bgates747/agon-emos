# Development log — 2026-09-14 UTC

## Executive summary

E05 cuts upload time33.41%; E06 finds only0.70% benefit from RX batching and
reverts it. All420native/candidate E06 checks pass, with a separately retained
heavy-observer failure. Physical restoration is not complete: original ESP
images are verified, but EMOS admission and original-ROM readback are pending.
A physical reset/screen observation is needed; E07 remains paused.

## INTEG-014 E03 — Stock reuse and execution-path review

Completed the Author-approved read-only audit after the E02 checkpoint. See
[the report](../../docs/tasks/INTEG-014/E03.md) and its linked symbol ledger.
E02 source/build/test input hashes still match; stock references remain clean.
No firmware implementation, build, deployment, reset or emulator change.

Current ELF confirms repeated per-attempt deadline/C/lock work, with a concrete
native-width C arithmetic candidate. Receive ISR performs parsing/effects
while applying RTS backpressure. The broad inventory distinguishes existing
stock reuse from necessary ownership/ABI adapters and new module functionality;
it does not label the full image increase as redundant. An older partial-write
finding and test-coverage statement were corrected against current source.

E03 checked off, E04 remains paused for supervised approval. No experimental
push; all preexisting source/profile/telemetry work remains unstaged. Hardware
spoken notification is the checkpoint cue, not performance validation.

## INTEG-014 E04 — Repeated physical baseline

Author accepted E03 and authorised controlled measurements. Frozen procedure
7a09db5 reused exact prior probe/app binaries with unchanged bench EMOS, no
rendering/browser output and no serial observer. Two full336 and two mixed36
matrices plus three wire4 invocations passed all756 exact rows; all independent
wire decodes passed. Zero snapshots. Source/build/test hashes remain unchanged.

[Results](../../docs/tasks/INTEG-014/E04.md) give paired milliseconds, scope
limits, all raw-value CSVs and repeatability. PRNG65535 endpoint medians were
589.602ms mainboard and2101.076ms Extender. P4 forward wire timing isolates
1530.514ms permitted idle; return isolates69.338ms Agon backpressure. E04
freezes the5% high-resolution improvement floor under its precommitted noise
rule; MOS-clock-only claims also need a33.333ms resolution floor.

Fresh flash preservation found a pre-existing coredump difference only; current
application matched stock. Dump retained. Original mainboard and P4 firmware
restored/independently verified; fresh native keyboard admission, SD roundtrip,
CLI COPY and unchanged startup/prior backup passed. No new EMOS flash or
source optimisation. Commit E04, send standard hardware voice cue, then stop
for E05 approval. No experimental push.

## INTEG-014 E05 — Measured C transmit improvement

E05 complete; [results](../../docs/tasks/INTEG-014/E05.md) lead with the executive
summary. Separate native-width, conditional clock-write and counted-loop changes
reduce65,535-byte Extender upload2,101.076→1,399.096ms(33.41%).384selected cases
and eight preliminaries pass; three independent wire decodes confirm1,396.612ms
forward and unchanged109.606ms return. Mainboard timing stable. The first block
loop's reload-during-stall difference was found, regression-tested and corrected
before deployment. No assembly comparison or parity claim. E06 proceeds under
the Author's explicit paired approval; temporary firmware pair remains installed.

## INTEG-014 E06 — Measurements complete; recovery checkpoint blocked

[E06 results](../../docs/tasks/INTEG-014/E06.md) preserve384exact transfer cases,
36native control/candidate cases, three independent captures and the rejected
observer results. Native service queries add about1% upload time. Batching
changes2→5bytes/service but receive time109.606→108.842ms misses the5% floor;
source change c0f5952 is reverted by f2562e8. Source/capture inputs are pinned.
The E05 C sender remains retained; no assembly rewrite, renderer or wire change.

Original-ROM image was verified on SD and flash command submitted, but reset
did not yield fresh keyboard admission. Original ESP images then restored and
independently verified; fresh admission still absent. No claim that the actual
eZ80 ROM or final CLI/SD is verified. Further mutations stop pending one human
physical reset/screen observation. Standard voice cannot be invoked; use the
authorised emulator fallback. E06 stays unchecked until recovery closes.
Generic build outputs restored/hash-checked; unrelated dirty inputs preserved.
Local commits only.

The existing validated Aginvadors emulator was launched as the authorised
notification fallback and labelled for a physical reset check. No emulator
profile files changed; this is not emulator validation. Hardware voice remains
unplayed and further hardware operations remain stopped. Final preservation
checks confirm unrelated inputs and generated builder outputs retained.


## INTEG-014 E07P — Unattended parity continuation

The Author authorized the hot bench unattended toward mainboard parity; the
frozen [contract](../../docs/tasks/INTEG-014/E07-parity.md) supersedes the E08
pause. No experimental push. Pre-run ROM/ESP/startup and all involved worktrees
are preserved. TX01 reduces large uploads from1070.062 to591.042 ms; paired
mainboard589.663 ms remains faster. RX01 reduces independently observed return
wire time109.608 to95.528 ms. Each passes376 exact physical cases with clean
recovery. Both targets remain open; these are substantial improvements, not
parity qualification. TX02 shorter branches, RX02 bounded FIFO drain and TX03
status classification are separate frozen candidates; only TX02 is currently
being measured on hardware. All91 host tests pass after correcting an existing
profile expectation to include the already-integrated telemetry unit. Complete
sender and IRQ instruction comparisons retain deadline/error/ABI boundaries.
