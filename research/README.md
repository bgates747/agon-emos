# EMOS research archive

This directory preserves dated research, implementation logs, and the original
implementation handoff. It is historical context, not the current task list or
repository ownership authority.

Current contracts are indexed by `docs/README.md`; unfinished work is indexed
only by `TODO.md`.
